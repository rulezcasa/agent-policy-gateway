// Overview: compliance stats, recent flagged actions, policy coverage at a glance.
export default function DashboardPage() {
  return <main className="p-8 text-xl font-semibold">Dashboard (placeholder)</main>;
}
