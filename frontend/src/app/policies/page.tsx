// List of ingested policies: category, status (active/draft), version, source doc.
export default function PoliciesPage() {
  return <main className="p-8 text-xl font-semibold">Policies (placeholder)</main>;
}
