// Admin uploads unstructured policy docs (PDFs). Kicks off LLM extraction on the backend.
export default function PolicyUploadPage() {
  return <main className="p-8 text-xl font-semibold">Upload Policies (placeholder)</main>;
}
