import { PolicyUploadDropzone } from "@/components/policies/policy-upload-dropzone";

export default function PolicyUploadPage() {
  return <div className="mx-auto max-w-4xl space-y-10"><section className="border-b border-slate-200/80 pb-7"><p className="text-xs font-medium uppercase tracking-[0.1em] text-indigo-600">Policy ingestion</p><h2 className="mt-3 text-[30px] font-medium tracking-[-0.045em] text-slate-950">Upload policy</h2><p className="mt-3 max-w-2xl text-[15px] leading-7 text-slate-500">Upload an unstructured company policy document. The connected gateway will extract and classify rules for you to review before they can be enforced.</p></section><PolicyUploadDropzone /><section className="border-t border-slate-200/80 pt-6"><p className="text-xs font-medium uppercase tracking-[0.1em] text-slate-500">Workflow</p><h3 className="mt-2 text-[18px] font-medium tracking-[-0.025em] text-slate-950">What happens next</h3><ol className="mt-6 grid gap-6 sm:grid-cols-4">{[["01", "Upload document", "Provide the original company policy PDF."], ["02", "Extract rules", "Text is extracted and rules are classified."], ["03", "Review results", "Confirm the structured rules against the source."], ["04", "Enforce policies", "Only confirmed active rules govern agent actions."]].map(([number, title, description]) => <li key={number} className="border-l border-slate-200 pl-4"><span className="font-mono text-xs font-medium text-indigo-600">{number}</span><p className="mt-2 text-[14px] font-medium text-slate-900">{title}</p><p className="mt-1 text-[13px] leading-5 text-slate-500">{description}</p></li>)}</ol></section></div>;
}
